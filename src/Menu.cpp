#include "Menu.h"

Menu::Menu(SDL_Rect display) : Object(display)
{
    myType = ObjectType::Menu;
    // TODO
    //SDL_SetTextureBlendMode( texture, SDL_BLENDMODE_BLEND);
    //SDL_SetTextureAlphaMod( texture, 160 );
}
